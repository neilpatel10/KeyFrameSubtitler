Required libraries:

cv2, PIL*, numpy, matplotlib*, moviepy(also needs ImageMagick)

*might not be required depending on inputs to the function

for ImageMagick you will need to find moviepy's directory and change the values in the config as described here: https://zulko.github.io/moviepy/install.html